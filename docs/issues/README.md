# Issues — gaps without an owner

Index of `docs/issues/`. Last reviewed 2026-09-21.

An **issue** names a gap. A **proposal** owns a design for closing one. The two are kept
apart because the failure mode they guard against is different: a proposal that opens with a
weak problem statement is hard to argue with, because the design is already on the table and
the reader is arguing against work someone did. An issue is the problem statement alone,
written before anyone is invested in an answer.

An issue must:

- **be reproducible from the code as it stands** — every claim cited to a file and line, or to
  a program in `raster-inference` that hits it;
- **say what it is not** — which existing proposal is adjacent, and precisely where that
  proposal's scope stops short of this;
- **stop before the design.** A §Directions section may sketch shapes and name their costs. It
  must not pick one. When a direction is picked, it graduates to `docs/proposals/` and the issue
  becomes a `Closed by:` line.

An issue that cannot yet be reproduced is a note, not an issue; keep it in the relevant
proposal's §Uncertainties instead.

## Open

Ordered by priority, not by date. The table's first row is the one to pick up next; the rest are
in the order they were opened. Priority is recorded on the issue's own `Status:` line as well, so
a reader arriving at the file does not have to come back here for it.

| issue | opened | subject | adjacent proposals |
| --- | --- | --- | --- |
| **[`selection-unbound-from-execution`](./selection-unbound-from-execution.md)** — **top priority** | 2026-09-16 | The only open **soundness** gap here; the rest fail closed. Two missing joins, one hinge: a selection proof is verified (`checks/store.rs:186-205`) and a replay journal is verified (`checks/io.rs:105-113`) and **nothing relates them**. (a) `lazy-list-recur` rule 8 is unimplemented — `grep -rn ListRange crates/raster-prover/guests/` is empty, so a chunked iteration's `ListRange.start`/`len`/`k` are never checked against the journal's position and `consumed_elements`, and coverage rests on the journal alone, which that proposal calls *"a binding, not an authority."* (b) `paged-bytes` §3.3 — the bytes a tile ran on are never equated with the bytes its selection proves, generically, for every Rastered type. Neither half closes the hole alone: with only (a) a prover replays arbitrary bytes at the right offset; with only (b) a prover re-reads `Range { start: 0, end: 2 }` five times over a `chunk = 2, L = 10` list and passes rules 1–7. Live in `examples/hello-tiles/src/main.rs:122-125`. | [`lazy-list-recur`](../proposals/lazy-list-recur.md), [`paged-bytes`](../proposals/paged-bytes.md), [`recur-progress-commitment`](../proposals/recur-progress-commitment.md), [`dynamic-index-selection`](../proposals/dynamic-index-selection.md) |
| [`recur-accumulator-slots`](./recur-accumulator-slots.md) | 2026-08-25 | No loop-carried slot is both readable and incrementally committed. `state` is readable and re-committed whole every iteration; `output` is incremental and unreadable. A loop whose write is a function of the accumulated value — §2's test — has no cheap home, and a data-dependent `Break` can only be decided from the expensive one. Narrowed 2026-08-25: only 2 of `raster-chain-inference`'s 5 recur tiles pass the test; the rest are program-side, see that repo's `append-shaped-accumulators`. | [`loop-carried-state`](../proposals/loop-carried-state.md), [`recur-sequence-break`](../proposals/recur-sequence-break.md), [`incremental-draft-witness`](../proposals/incremental-draft-witness.md) |
| [`authenticated-chain-draft-output`](./authenticated-chain-draft-output.md) — **now blocks the fraud path too** | 2026-08-27 | A finalized `Draft` has no object in the recorder's storage replica, so anything asking storage for it at `[DRAFT_NAMESPACE, n]` fails. Two callers reach it: the output-selection replay (`recorder.rs:642`) on `examples/chain-example` stage 3, and — **found 2026-09-18** — `build_storage_selection_witnesses` (`run.rs:765`) on `examples/hello-tiles`, where the draft is a tile *input* and the program is not a chain. Widened scope rules out "forbid draft program outputs" as a fix. Fails closed, not a soundness hole, but on `hello-tiles` it is the last blocker between a *detected* divergence (audit exits `101`) and a completed fraud proof: the honest path commits and audits clean. Invisible because `chain_stage_cli.rs` only ever runs `--no-auth`, which installs no recorder. Confirmed pre-existing at `359582f`, and again 2026-09-24 against restored pre-refactor sources while landing [`storage-role-split`](../proposals/storage-role-split.md) — identical panic, coordinates and stage-2 hashes. **⚠️ The `hello-tiles` reproducer is no longer fast** (2026-09-24): the audit now proceeds past the witness build into proving, so whether that caller is fixed or merely moved is unresolved — see the issue's note. | [`chain-stage-execution`](../proposals/chain-stage-execution.md), [`chain-io-commitment`](../proposals/chain-io-commitment.md), [`incremental-draft-witness`](../proposals/incremental-draft-witness.md) |
| [`sequence-scope-forbids-narrowing`](./sequence-scope-forbids-narrowing.md) — **blocks the fraud path** | 2026-09-21 | A sub-sequence may not `select!` into its own parameter. `InputBinding::SequenceScope` carries `input_index` and no selector (`cfs.rs:827`), so the CFS cannot say "narrowed by path P"; the guest therefore `assert_eq!`s the whole `StorageData` (`checks/cfs.rs:59`, `:706`) and a consumer that narrows `personal_data` to `.name` fails — same `source_root_hash`, selector `[personal_data_bin, name]` vs `[personal_data_bin]`, `selected_len` 21 vs 694. The shape is the one the authoring skill §2 requires (*"the field, not the struct"*). Live at `examples/hello-tiles/src/main.rs:25,67`. Honest path unaffected; reached only from `prove()`. **Found by a fraud probe that drew a different random victim step than the one behind `authenticated-chain-draft-output`** — so the number of remaining blockers on that path is unknown, not one. Deterministic reproducer since 2026-09-21: `--fraud-step 45` (`--fraud-step list` shows all 58 eligible steps). This one costs ~2.5 h — the panic is inside the guest, so it lands only after the window is proven. | [`selection-unbound-from-execution`](./selection-unbound-from-execution.md), [`draft-provenance`](../proposals/draft-provenance.md), [`sequence-grammar-closure`](../proposals/sequence-grammar-closure.md) |
| [`counting-forked-from-profiling`](./counting-forked-from-profiling.md) | 2026-08-31 | Two accumulators answer "what did this run execute". `profiling` carries mode-independent fields (`tile_id`, `coordinates`, `input_bytes`) but is refused whole-artifact when unauthenticated (`unauthenticated-execution` §6.2, a ruling about *timings*), so `tile_census` re-derives a subset from a second hook with different gating into an untyped hand-built JSON — no `version`, no `run_id` to join the two, and `total_tile_executions` an identity over `trace_events` rather than a cross-check. Its "both authentication modes" header holds for the `tiles` map and not the `trace_events` one: four recur variants are structurally zero under `--no-auth` (`raster-macros/src/recur.rs:733`, `:856`, `:1251`) and nothing in the artifact says so. Not a soundness issue. `tile_census.rs` is untracked on `feature/recur-defered-finilize`. | [`unauthenticated-execution`](../proposals/unauthenticated-execution.md), [`artifact-inspection`](../proposals/artifact-inspection.md), [`trace-event-vocabulary`](../proposals/trace-event-vocabulary.md) |
| [`chain-shape-count-unverified`](./chain-shape-count-unverified.md) | 2026-08-31 | A `--stage` re-run takes its repeat-block counts from the run directory's `chain-shape` sidecar and checks them only against the manifest — `spec_digest` (`chain.rs:755`), `max` and producer position (`chain/expand.rs:271`, `:278`). The count itself is compared against nothing, though the producing stage's `output.bin` is in the same directory and `read_trip_count` (`chain.rs:806`) already decodes it without executing anything, and `verify_shape` re-derives every count from the producer (`chain/expand.rs:482-491`). A plausible altered count expands, executes, invalidates downstream and rewrites the sidecar against a graph the commitment `--stage` deliberately leaves untouched does not describe. Needs no tampering in one case: `--stage` on the count producer records the pre-run count over the artifact the run just wrote. | [`chain-repeat`](../proposals/chain-repeat.md), [`chain-io-commitment`](../proposals/chain-io-commitment.md), [`chain-stage-execution`](../proposals/chain-stage-execution.md) |
| [`fraud-evidence-storage-unavailable`](./fraud-evidence-storage-unavailable.md) | 2026-09-16 | **Fraud is detected and then cannot be proven** — the CLI panics building the receipt. `prove()` asks the recorder for a storage selection witness per window step (`run.rs:830`, `:851`); the lookup misses in the auditing process's own storage map and the `Err` becomes a `panic!` inside an `unwrap_or_else` (`run.rs:767` ← `raster-runtime/src/storage.rs:562`). That map is insert-only with a duplicate-write assert and no `remove`, so the object was never written by *either* run — the trace records an input binding whose source coordinates no execution backs. Reproduced on `prompt-prepare` at window 32 both by a hand byte-flip and by the built-in `fraud()` injector (`run.rs:528`), same binding and coordinates `[5, 0, 56]`. Fails closed, so not a soundness hole — but on the dispute path a completeness defect is a lost dispute. Same class as `fraud-window-trace-ends` §2's `witness_record_inputs` panic, which ran earlier and is fixed; whether that fix exposed this is untested and cheap to settle. | [`chain-io-commitment`](../proposals/chain-io-commitment.md), [`trace-leaf-field-binding`](../proposals/trace-leaf-field-binding.md), [`selection-unbound-from-execution`](./selection-unbound-from-execution.md) |

## Closed

| issue | opened | closed | subject | closed by |
| --- | --- | --- | --- | --- |
| [`fraud-window-trace-ends`](./fraud-window-trace-ends.md) | 2026-09-11 | 2026-09-16 | The 128-bit guarantee is per *window*, and neither end of a trace has one — at the head the window built was unprovable (a fingerprint slice one entry short **and** one item late, declared as `window_size` regardless); at the tail detection had no forward runway and degraded to `bits_per_item`, 2⁻¹ at `window_size >= 128`. Both closed. The head now clamps and asserts the genesis opening state instead of a margin it cannot have; the tail's trace roots are revealed in full, so detection there no longer reads the fingerprint and `window_size` stays **one** parameter. The `witness_record_inputs` panic §2 records as a separate defect is fixed too — and fixing it surfaced three soundness breaks the issue's §4 could not see from the window's arithmetic: `exec_index` and `sequence_id` reached the trace leaf unverified, the sequence-scope witness was bound to nothing, and the window's shape was unbound. Format break: `TRACE_COMMITMENT_DOMAIN` v1 → v2. | [`trace-end-windows`](../proposals/trace-end-windows.md), [`trace-leaf-field-binding`](../proposals/trace-leaf-field-binding.md) |
